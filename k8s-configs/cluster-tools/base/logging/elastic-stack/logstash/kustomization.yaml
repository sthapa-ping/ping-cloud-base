kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
- storage.yaml
- logstash.yaml
- hpa.yaml
- unused-pv-cleanup.yaml
- secret.yaml
- pdb.yaml
- customer_pipelines

patches:
  # Toggling commenting of this patch is controlled by git-ops-command.sh 
  #- path: patch-opensearch-bootstrap-pf-impossible-login.yaml
