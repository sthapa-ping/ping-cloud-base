#
# This defines the pingcloud-metadata deployment.
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingcloud-metadata
  labels:
    role: pingcloud-metadata
spec:
  selector:
    matchLabels:
      role: pingcloud-metadata
  template:
    metadata:
      name: pingcloud-metadata
      labels:
        role: pingcloud-metadata
    spec:
      serviceAccount: ping-serviceaccount
      containers:
      - name: pingcloud-metadata
        image: public.ecr.aws/r2h3l6e4/pingidentity/pingcloud-metadata:v0.1
        imagePullPolicy: IfNotPresent
        env:
        - name: NAMESPACE
          value: ping-cloud
        envFrom:
        - configMapRef:
            name: pingcloud-metadata-environment-variables
        resources:
          limits:
            memory: "400Mi"
            cpu: "200m"
          requests:
            memory: "400Mi"
            cpu: "200m"
        readinessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 60
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 60
          periodSeconds: 30
          failureThreshold: 3
        ports:
        - containerPort: 5000
